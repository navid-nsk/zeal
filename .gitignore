# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
env/

# --- data & results live on figshare, not in git ---
# unpack the figshare data bundle into ./data (see README); it is not committed.
/data/
/raw/

# --- trained weights / scratch arrays (regenerated by the pipeline) ---
*.pt
*.npy

# --- OS ---
.DS_Store
Thumbs.db
